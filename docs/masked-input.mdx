---
title: Masked Input
---

import {useState} from 'react'
import { Playground, Props } from 'docz'
import  MaskedInput from '../src/masked-input/masked-input.tsx';

# Masked Input

```javascript
import { MaskedInput } from 'daybreak';
```

## Basic usage

<Playground>
  {()=>{
    const [value, setValue] = useState('');
    const [value2, setValue2] = useState('');
    return (
      <div>
        <MaskedInput
          mask="(###) ###-####"
          value={value}
          onChange={(val) => { setValue(val); }}
        />
        <br />
        <br />
        <MaskedInput
          clearable
          onClear={() => setValue2('')}
          mask="### ### ###"
          value={value2}
          onChange={(val) => { setValue2(val); }}
        />
      </div>
    )
  }}
</Playground>

## Props

<Props of={MaskedInput} />
