---
title: Dialog
---

import {useState} from 'react'
import { Playground, Props } from 'docz'
import Example from '../site/examples/dialog.example.tsx'
import {
  Dialog,
  Alert,
  Confirm,
  Modal,
} from '../src/dialog/dialog.tsx'
import Button from '../src/button/button.tsx'

# Dialog


```javascript
import { Dialog } from 'daybreak';
```

## Basic usage

<Playground>
{()=>{
  const [visible, setVisible] = useState(false);
  return (
    <div>
      <Button type="button" onClick={() => setVisible(!visible)}>Open</Button>
      <Dialog
        visible={visible}
        buttons={
          [
            <Button type="button" onClick={() => setVisible(false)}>Yes</Button>,
            <Button type="button" onClick={() => setVisible(false)}>No</Button>,
          ]
        }
        onClose={() => setVisible(false)}
      >
        hello
      </Dialog>
    </div>
  )
}}
</Playground>

## Don't Close When Click the Mask

<Playground>
{()=>{
  const [visible, setVisible] = useState(false);
  return (
    <div>
      <Button type="button" onClick={() => setVisible(!visible)}>Open</Button>
      <Dialog
        visible={visible}
        buttons={
          [
            <Button type="button" onClick={() => setVisible(false)}>Yes</Button>,
            <Button type="button" onClick={() => setVisible(false)}>No</Button>,
          ]
        }
        onClose={() => setVisible(false)}
        closeOnClickMask={false}
      >
        hello
      </Dialog>
    </div>
  )
}}
</Playground>

## Don't Close When the 'Esc' key is pressed

<Playground>
{()=>{
  const [visible, setVisible] = useState(false);
  return (
    <div>
      <Button type="button" onClick={() => setVisible(!visible)}>Open</Button>
      <Dialog
        visible={visible}
        buttons={
          [
            <Button type="button" onClick={() => setVisible(false)}>Yes</Button>,
            <Button type="button" onClick={() => setVisible(false)}>No</Button>,
          ]
        }
        onClose={() => setVisible(false)}
        closeOnEsc={false}
      >
        hello
      </Dialog>
    </div>
  )
}}
</Playground>

## Prevent background scrolling

<Playground>
{()=>{
  const [visible, setVisible] = useState(false);
  return (
    <div>
      <Button type="button" onClick={() => setVisible(!visible)}>Open</Button>
      <Dialog
        visible={visible}
        buttons={
          [
            <Button type="button" onClick={() => setVisible(false)}>Yes</Button>,
            <Button type="button" onClick={() => setVisible(false)}>No</Button>,
          ]
        }
        onClose={() => setVisible(false)}
        preventBackgroundScrolling
      >
        hello
      </Dialog>
    </div>
  )
}}
</Playground>

## Modal

| param | type |
| --- | --- |
| content | `React.ReactNode` |
| buttons? | `Array<React.ReactElement>` |
| onClose? | `Function` |
| override? | `{ [k: string]: any; }` |

<Playground>
  {()=>{
    const openModal = () => {
      const close = Modal(
        <p>This is a modal.</p>,
        [<Button type="button" onClick={() => close()}>Close</Button>]
      );
    };
    return (
      <Button type="button" onClick={openModal}>Open</Button>
    )
  }}
</Playground>

## Alert

| param | type |
| --- | --- |
| content | `React.ReactNode` |
| override? | `{ [k: string]: any; }` |

<Playground>
  <Button type="button" onClick={() => Alert('This is an alert.')}>Open</Button>
</Playground>

## Confirm

| param | type |
| --- | --- |
| content | `React.ReactNode` |
| onOk? | `Function` |
| onCancel? | `Function` |
| override? | `{ [k: string]: any; }` |

<Playground>
  <Button type="button" onClick={() => Confirm('This is an confirm.')}>Open</Button>
</Playground>

## Props

### Dialog

<Props of={Dialog} />
