---
title: Switch
---

import {useState} from 'react'
import { Playground, Props } from 'docz'
import Switch from '../src/switch/switch.tsx'

# Switch

```javascript
import { Switch } from 'daybreak';
```

## Standalone

<Playground>
  {()=>{
    const [value, setValue] = useState(true);
    return (
      <div>
        <Switch checked={value} onChange={(e) => setValue(e.target.checked)}></Switch>
      </div>
    );
  }}
</Playground>

## With Children

<Playground>
  {()=>{
    const [value, setValue] = useState(true);
    return (
      <div>
        <Switch checked={value} onChange={(e) => setValue(e.target.checked)}>Enable?</Switch>
      </div>
    );
  }}
</Playground>
