---
title: Icon
---

import { Playground, Props } from 'docz';
import Icon from '../src/icon/icon.tsx';
require('../src/icon/importAllIcons.tsx');

# Icon

```javascript
import { Icon } from 'daybreak';
```

## Basic usage

<Playground>
  <Icon name="alipay"></Icon>
  <Icon name="qq"></Icon>
</Playground>

## Available Icons

export const data = [
  'alipay',
  'caution',
  'clear',
  'download',
  'hide',
  'info',
  'left',
  'loading',
  'qq',
  'right',
  'settings',
  'show',
  'thumbs-up',
  'upload',
  'wechat',
];

export const IconsTable = (props) => {
  const { data } = props;
  return (
    <table>
      <thead>
        <tr>
          <th>name</th>
          <th>icon</th>
        </tr>
      </thead>
      <tbody>
        {data.map((item, index) => (
          <tr key={`${item} ${index}`}>
            <td>{item}</td>
            <td>
              <Icon name={item}></Icon>
            </td>
          </tr>
        ))}
      </tbody>
    </table>
  );
};

<IconsTable data={data} />
