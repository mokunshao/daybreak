---
title: Checkbox
---

import {useState} from 'react'
import { Playground, Props } from 'docz'
import Checkbox from '../src/checkbox/checkbox.tsx'

# Checkbox

## Standalone

<Playground>
  {()=>{
    const [checked1, setChecked1] = useState(true);
    return (
      <div>
        <Checkbox checked={checked1} onChange={(e) => setChecked1(e.target.checked)} />
      </div>
    );
  }}
</Playground>

## With children

<Playground>
  {()=>{
    const [checked2, setChecked2] = useState(false);
    return (
      <div>
        <Checkbox checked={checked2} onChange={(e) => setChecked2(e.target.checked)}>🤔️Agree?</Checkbox>
      </div>
    );
  }}
</Playground>

## Props

<Props of={Checkbox} />
